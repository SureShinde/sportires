<?php
/**
 * @var $block \ITM\Sportires\Block\Runflatfilter
 */
?>

<?php

    $runflate = $this->getRequest()->getParam('run_flat');
    if($runflate == null || $runflate == 770) {
        $yesno = 0;
        $datapost="769";
    }else{
        $yesno = 1;
        $datapost="770";
    }

    $_query = array('run_flat' => $datapost);
    $current_url = $block->getUrl('*/*/*', array('_current' => true,'_use_rewrite' => true, '_query' => $_query));
?>

<div class="runflat-filter">
    <span class='custom-tittle'><?=__('RunFlat')?></span>
    <div class="center">
        <input type="checkbox" <?php if($yesno == 1){ echo 'checked="checked"'; }  ?> class="cbx-runflat" id="cbx" style="display:none"/>
        <label for="cbx" class="toggle"><span></span></label>
    </div>
</div>

<script>
    require(['jquery'], function($) {
        jQuery(document).ready(function () {
            $(".cbx-runflat").click(function(){
                window.location.href = '<?= $current_url ?>';
            });

        });
    });
</script>